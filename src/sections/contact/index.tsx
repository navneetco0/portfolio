import { Section } from "../../@components";
import { project_data } from "../../@assets";

const Contacts = () => {
  return (
    <Section
      id="contact"
    />
  );
};

export default Contacts;
